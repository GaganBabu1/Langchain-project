# Langchain-project
# PDF Question Answering with RAG

This project implements a Retrieval-Augmented Generation (RAG)
pipeline using LangChain, FAISS, and HuggingFace models.

## Install

pip install -r requirements.txt

## Run

python src/stories.py
